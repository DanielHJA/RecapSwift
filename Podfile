# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RecapSwift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RecapSwift

  pod ‘Alamofire’
  pod ‘ObjectMapper’ 
  pod ‘Kingfisher’
  pod 'YouTubePlayer'

  target 'RecapSwiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecapSwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
